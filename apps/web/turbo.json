{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", ".env", ".env.*", "../../packages/ui/src/**/*.{ts,tsx,css}"],
      "outputs": [".next/**", "!.next/cache/**"]
    }
  }
}
